@model IEnumerable<DomingoRoofWorks.Models.EmployeeJob>

@{
    ViewData["Title"] = "Index";
}

<h1>List Of Employee Jobs</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CardNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CardNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeJobID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeJobID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeJobID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
